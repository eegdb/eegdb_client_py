# Dependencies for eegdb_client_py
numpy>=1.24
pyedflib>=0.1.38
PyQt6>=6.6
mne>=1.6
curryreader>=0.1.0

# Optional: local compressed download decode (--local-decode)
# Build from sibling eegdb-codec repo:
#   cd ../eegdb-codec && make codec-wheel
#   pip install dist/python/eegdb_codec-*.whl
# eegdb_codec
